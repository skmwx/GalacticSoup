import './global.css';

/**
 * Importing this module installs the global stylesheet and presentation tokens.
 * The constant exists so the import is a deliberate, traceable dependency
 * rather than a bare side-effect import that a tool might drop.
 */
export const GLOBAL_STYLES_HREF = 'ui/styles/global.css';
