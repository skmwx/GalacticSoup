/**
 * Localization for the interface layer (Technical Specification 12.5).
 */
export { catalogFor, CATALOGS, DEFAULT_LOCALE } from './catalog';
export { LocalizationProvider, useLocalizer, useTranslate } from './LocalizationProvider';
export type { LocalizationProviderProps } from './LocalizationProvider';
